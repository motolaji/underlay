# contracts

This directory is scaffolded for a Foundry-only workflow.

Foundation work included:

- `foundry.toml`
- `src/interfaces/`
- `src/libraries/`
- `src/libraries/VaultConfig.sol`
- `test/`
- `script/`

The local machine does not currently have `forge` installed, so dependency installation and compile validation are deferred.
