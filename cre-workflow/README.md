# cre-workflow

This package scaffolds Underlay's Chainlink CRE workflow surface.

Notes:

- Package management uses npm for the monorepo
- CRE still requires the external `cre` CLI for simulation and deployment
- The local machine does not currently have the `cre` CLI installed
- Workflow implementation remains a later round; this scaffold keeps the folder structure and command surface in place
